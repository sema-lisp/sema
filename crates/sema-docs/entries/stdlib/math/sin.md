---
name: "sin"
module: "math"
section: "Trigonometry"
params: [{ name: x, type: number, doc: "angle in radians" }]
returns: "float"
see_also: ["cos", "math/tan", "math/asin"]
---

Sine (argument in radians).

```sema
(sin 0)       ; => 0.0
(sin pi)      ; => ~0.0
```
