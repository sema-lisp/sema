---
name: "prompt/messages"
module: "prompt"
params: [{ name: prompt, type: prompt }]
returns: "list"
---

Return the prompt's messages as a list of message values.

```sema
(prompt/messages my-prompt)
```
