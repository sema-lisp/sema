(def should-error (state false))

(define (maybe-error-view)
  (if @should-error
    (error "intentional render error")
    [:p {:id "content" :data-testid "content"} "OK"]))

(mount! "#app" "maybe-error-view")
